'use strict';

/**
 * topbar-announcement service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::topbar-announcement.topbar-announcement');
