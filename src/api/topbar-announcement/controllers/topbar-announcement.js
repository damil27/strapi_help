'use strict';

/**
 * topbar-announcement controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::topbar-announcement.topbar-announcement');
