'use strict';

/**
 * topbar-announcement router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::topbar-announcement.topbar-announcement');
